import { MatVerticalStepperScrollerDirectiveDirective } from './mat-vertical-stepper-scroller-directive.directive';

describe('MatVerticalStepperScrollerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MatVerticalStepperScrollerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
